import { WaItemsFilterPipePipe } from './wa-items-filter-pipe.pipe';

describe('WaItemsFilterPipePipe', () => {
  it('create an instance', () => {
    const pipe = new WaItemsFilterPipePipe();
    expect(pipe).toBeTruthy();
  });
});
